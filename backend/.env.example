# Backend configuration
# Update these values and copy to .env in the same directory to override defaults.

# Express server port
PORT=5001

# Pose service settings
# Host should include scheme; omit trailing slash.
POSE_SERVICE_HOST=http://127.0.0.1
POSE_SERVICE_PORT=5002
# Optional: supply a fully qualified base URL (without /detect-pose) if the pose service is hosted elsewhere.
# POSE_SERVICE_URL=http://127.0.0.1:5002

# Python interpreter used to launch pose_server.py automatically
POSE_PYTHON=python3

# Set to false to skip automatically starting the pose service
AUTO_START_POSE=true
